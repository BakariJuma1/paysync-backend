services:
  - type: web
    name: paysync-backend
    env: python
    rootDir: paysync-backend/server
    buildCommand: pip install -r requirements.txt
    startCommand: flask db upgrade && gunicorn wsgi:app
    plan: free
    envVars:
      - fromGroup: paysync-env
