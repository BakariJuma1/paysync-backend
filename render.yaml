services:
  - type: web
    name: paysync-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: |
      cd ..
      PYTHONPATH=. flask --app server.app db upgrade && gunicorn server.wsgi:app
    rootDir: server
